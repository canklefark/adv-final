---
const { tabData, ariaLabel, activeCreature } = Astro.props;
---

<nav class="tab-list | flex" role="tablist" aria-label={ariaLabel}>
  {
    tabData.map((data) => (
      <a
        class="ff-body uppercase text-dark fw-bold"
        href={data.url}
        data-active-tab={activeCreature === data.name ? "true" : "false"}
        data-image={`${data.name.toLowerCase()}-image`}>
        {data.name}
      </a>
    ))
  }
</nav>

<style>
  .tab-list {
    --gap: 0;
  }

  a {
    position: relative;
    text-align: center;
    background-color: var(--clr-base);
    width: 100%;
    padding: var(--space-s) var(--space-m);
    border-radius: 9px 9px 0 0;
    box-shadow: 0px 0px 10px 2px var(--shadow-primary);
    display: block;
    transform: translateY(0.4rem);
    transition: transform 0.3s ease;

    &:hover:not([data-active-tab="true"]),
    &:focus-within:not([data-active-tab="true"]) {
      transform: translateY(0);
    }

    &:nth-child(1) {
      z-index: 3;
    }

    &:nth-child(2) {
      z-index: 2;
    }

    &:nth-child(3) {
      z-index: 1;
    }

    &[data-active-tab="true"] {
      transform: translateY(0);
      background-color: var(--clr-base-l-1);
      box-shadow: 0px 0px 10px 4px var(--shadow-primary);
      z-index: 4;
    }
  }
</style>
